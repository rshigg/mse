// SQLite operations
export enum OPERATIONS {
  CREATE = 'CREATE',
  INSERT = 'INSERT',
  UPDATE = 'UPDATE',
  DELETE = 'DELETE',
}

export const dbPath = '/sql/mse.sqlite';

export const dbVersion = 1;
